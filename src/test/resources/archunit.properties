freeze.store.default.path=src/test/resources/archunit-store
# 常态禁止自动建 store：规则改动/新增冻结规则时若缺 store 条目必须显式失败，
# 防止静默重冻结。一次性生成 store 用：
# mvn test -Dtest=ArchitectureTest -Darchunit.freeze.store.default.allowStoreCreation=true
freeze.store.default.allowStoreCreation=false
